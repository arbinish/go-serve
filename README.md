# go-serve
HTTP serve from local directories

# usage
```bash
$  go-serve -help
Usage of go-serve:
  -path string
    	path to serve (default ".")
  -port string
    	Port to listen (default "8085")
```
